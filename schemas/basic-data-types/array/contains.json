{
  "$id": "/schemas/basic-data-types/array/contains.json#",
  "type": "array",
  "contains": {
    "type": "number"
  },
  "title": "Array Type Schema (Contains Keyword)",
  "description": "Demonstrates an array type schema using the `contains` keyword.\n\n * While the items schema must be valid for every item in the array, the contains schema only needs to validate against one or more items in the array."

}